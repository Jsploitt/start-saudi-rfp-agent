# Required. The demo will not start without it unless DEMO_MODE=cached.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. "live" (default) or "cached" to replay fixtures/cached-run.json offline.
DEMO_MODE=live

# Optional. Server port, default 5173.
PORT=5173
